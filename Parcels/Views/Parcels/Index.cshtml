@using Parcels.Models;

<h1>Packaging en route</h1>
@if (Model.Count == 0)
{
  <p>There are no packages en route.</p>
}
<ul>
@foreach (Parcel parcel in Model)
{
  <li>@parcel.Description</li>
}
</ul>

<a href="/parcels/new">Ship Package Here.</a>